import { FaMoneyCheckAlt } from "react-icons/fa";

const CartWidget = () => {
  return (
    <div>
      <FaMoneyCheckAlt size={30} />
      <span>0</span>
    </div>
  );
};

export default CartWidget;
